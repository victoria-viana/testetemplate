<%= form_with(model: tipo_entrada, local: true) do |form| %>
  <% if tipo_entrada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_entrada.errors.count, "error") %> prohibited this tipo_entrada from being saved:</h2>

      <ul>
      <% tipo_entrada.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
